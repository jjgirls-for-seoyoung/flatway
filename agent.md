# 🤖 FlatWay 개발 AI 에이전트 공통 행동 규칙 (agent.md)

본 문서는 **FlatWay** 프로젝트의 개발을 진행하는 AI 에이전트가 협업 및 코드 작성 과정에서 반드시 준수해야 하는 공통 규칙과 지침을 정의합니다. 에이전트는 작업을 시작하거나 코드를 수정하기 전에 본 규칙을 먼저 확인하고 행동해야 합니다.

---

## 1. 언어 및 소통 규칙 (Language & Communication)
* **한국어 출력 우선:** 사용자 및 다른 개발자와의 모든 대화, 피드백, 응답은 한국어(한글)로 작성해야 합니다.
* **명확하고 간결한 설명:** 구현 내용이나 에러 수정 사항을 보고할 때, 복잡한 코드 나열보다는 논리적 설계와 핵심 동작 방식을 먼저 한국어로 친절하게 설명해야 합니다.

---

## 2. 프로젝트 문서 분석 우선 지침 (Documentation Analysis First)
* **기존 문서 참고 필수:** 새로운 작업을 인계받거나 코드를 수정할 때, 프로젝트 내에 이미 작성된 모든 마크다운 문서(`.md` 파일)들을 최우선으로 탐색하고 정독해야 합니다.
  * 참고 대상: `dashboard/docs/` 내 설계 문서, `docs/` 내 워크시트 PDF(변환 텍스트), `단차수집_인수인계_가이드.md` 등.
* **기존 설계 존중:** 신규 코드나 기능을 추가할 때 이미 합의된 데이터베이스 스키마(예: `hazards`, `buildings` 테이블), 변수 명명 규칙, 그리고 아키텍처 흐름에서 벗어나지 않도록 설계 일관성을 유지해야 합니다.
* **수정 내역 문서 갱신:** 코드 구조나 데이터 규격을 변경할 경우, 해당 변경 사항을 관련 인수인계 문서 및 README에 즉시 갱신해 주어야 합니다.

---

## 3. 코드 작성 및 수정 원칙 (Coding Standards)
* **주석 및 문서 유지 (Documentation Integrity):** 기존의 한국어/영어 주석 및 함수 설명(docstring)을 임의로 제거하거나 파괴하지 않고 최대한 보존해야 합니다.
* **Next.js 대시보드 스타일 지침:**
  * Vanilla CSS를 사용하여 다크 모던 테마와 Glassmorphism 효과를 유지합니다.
  * 반응형 웹 디바이스 규격(모바일 768px 이하 대응 구조)에 맞게 그리드 및 정적 포지션을 신중하게 설계해야 합니다.
  * Leaflet 지도 라이브러리 사용 시 SSR 오류 방지를 위해 `next/dynamic` 비활성화 렌더링 규칙을 고수합니다.
* **Flutter 모바일 앱 스타일 지침:**
  * GPS 및 센서(`sensors_plus`) 수집 파이프라인의 수치 보정 매개변수를 유동적으로 관리합니다.
  * UI 구조 변경 시 전체화면 지도 렌더링을 위해 `Positioned.fill` 또는 `Expanded` 위젯 배치를 준수합니다.

---

## 4. 데이터베이스 및 API 연동 규칙 (Database & API)
* **Supabase 자격 증명 체크:** `.env.local`이나 모바일 설정 파일의 API 자격 증명이 유효하지 않을 경우, 자동으로 Local Storage나 로컬 목 데이터 모드로 전환(Fallback)되도록 예외 처리를 누락하지 않아야 합니다.
* **스키마 변경 제한:** 테이블 컬럼을 수정해야 하는 경우, 반드시 `supabase_schema.sql` 파일을 먼저 갱신하고 스키마 마이그레이션 호환성을 검증해야 합니다.

---

## 5. 형상 관리 (Git / Version Control)
* **브랜치 활용:** 메인 브랜치(`main`)에 직접 푸시하기 전, 명확한 작업 단위 브랜치(예: `feature/dashboard-update`)에서 먼저 검증 및 빌드 테스트를 수행해야 합니다.
* **의미 있는 커밋 메시지:** 커밋 시 커밋 프리픽스(`feat:`, `fix:`, `docs:`, `style:`)를 한글 설명과 함께 적절히 기입하여 작업 이력을 투명하게 남깁니다.
